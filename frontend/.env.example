# Registry domain — must match the domain configured on the server.
# Used in generated install commands and module URLs shown in the UI.
# Example: NEXT_PUBLIC_DOMAIN=buf.example.com
NEXT_PUBLIC_DOMAIN=example.com

# Backend URL for server-side RPC proxy (internal only).
BACKEND_URL=http://localhost:50051

// Commit, diff, and tree API for the Hades schema registry.
//
// A commit represents a single push of .proto files to a module. Each commit
// stores a content digest (B5 format) so clients can verify integrity after
// download. Commits are immutable once created.
//
// This file also covers file-level diffs and repository tree browsing, which
// are logically part of the commit history domain.

// @generated by protoc-gen-connect-es v1.6.1 with parameter "target=ts"
// @generated from file api/registry/v1/commit.proto (package hades.api.registry.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { GetCommitDiffRequest, GetCommitDiffResponse, GetCommitRequest, GetCommitResponse, GetFileContentRequest, GetFileContentResponse, ListCommitsRequest, ListCommitsResponse, ListModuleFilesRequest, ListModuleFilesResponse } from "./commit_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * CommitService exposes commit history, file diffs, and tree browsing for schema modules.
 *
 * Commits are immutable once created. The digest field allows clients to
 * verify content integrity independent of the transport layer.
 *
 * Every RPC here is readable anonymously and enforces read access per module,
 * so a private module a caller cannot read is reported as NOT_FOUND rather
 * than PERMISSION_DENIED. Commit hashes are unique registry-wide, so any RPC
 * taking one also checks that the commit belongs to the named module.
 *
 * @generated from service hades.api.registry.v1.CommitService
 */
export const CommitService = {
  typeName: "hades.api.registry.v1.CommitService",
  methods: {
    /**
     * ListCommits returns a page of commits for the given module, newest first.
     * Returns NOT_FOUND if the module does not exist or the caller cannot read it.
     *
     * @generated from rpc hades.api.registry.v1.CommitService.ListCommits
     */
    listCommits: {
      name: "ListCommits",
      I: ListCommitsRequest,
      O: ListCommitsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetCommit returns a single commit by its Git commit hash.
     * Returns NOT_FOUND if the commit does not exist or the caller cannot read
     * the module it belongs to.
     *
     * @generated from rpc hades.api.registry.v1.CommitService.GetCommit
     */
    getCommit: {
      name: "GetCommit",
      I: GetCommitRequest,
      O: GetCommitResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetCommitDiff returns the file-level diff for a commit vs its parent.
     * The initial commit is diffed against an empty tree so all files appear
     * as additions. Returns NOT_FOUND if the commit hash does not exist.
     *
     * @generated from rpc hades.api.registry.v1.CommitService.GetCommitDiff
     */
    getCommitDiff: {
      name: "GetCommitDiff",
      I: GetCommitDiffRequest,
      O: GetCommitDiffResponse,
      kind: MethodKind.Unary,
    },
    /**
     * ListModuleFiles returns the depth-1 contents of a directory in the module
     * repository, read from commit_hash or from the default branch head when
     * that is empty.
     *
     * Returns NOT_FOUND if the module or path does not exist, or if commit_hash
     * is unknown or belongs to a different module.
     *
     * @generated from rpc hades.api.registry.v1.CommitService.ListModuleFiles
     */
    listModuleFiles: {
      name: "ListModuleFiles",
      I: ListModuleFilesRequest,
      O: ListModuleFilesResponse,
      kind: MethodKind.Unary,
    },
    /**
     * GetFileContent returns the raw content of a single file by its path, read
     * from commit_hash or from the default branch head when that is empty.
     * Returns NOT_FOUND if the module, commit, or file path does not exist.
     *
     * @generated from rpc hades.api.registry.v1.CommitService.GetFileContent
     */
    getFileContent: {
      name: "GetFileContent",
      I: GetFileContentRequest,
      O: GetFileContentResponse,
      kind: MethodKind.Unary,
    },
  }
} as const;


// CI (lint and breaking-change) run results for the Hades schema registry.
//
// When a commit is pushed, the upload handler runs buf lint, and buf breaking
// against the previous commit when the module has breaking checks enabled.
// Both run synchronously and a violation rejects the push, so a commit that
// exists in the registry has already passed the checks configured for its
// module at that time. The result is recorded as a CIRun keyed by
// (module, commit_hash), in the same transaction that creates the commit.
//
// It follows that every stored CIRun has passed. A rejected push produces no
// commit, and a record is keyed by the commit hash, so there is nothing to
// attach a failure to. The lint_errors and breaking_errors fields exist for a
// future non-blocking check mode and are empty today. No record is written at
// all when linting is disabled for the deployment: a row claiming "passed" for
// checks that never ran would be worse than no row.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: api/registry/v1/ci.proto

package registryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/alipourhabibi/Hades/api/gen/api/registry/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CIServiceName is the fully-qualified name of the CIService service.
	CIServiceName = "hades.api.registry.v1.CIService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CIServiceGetCIRunProcedure is the fully-qualified name of the CIService's GetCIRun RPC.
	CIServiceGetCIRunProcedure = "/hades.api.registry.v1.CIService/GetCIRun"
)

// CIServiceClient is a client for the hades.api.registry.v1.CIService service.
type CIServiceClient interface {
	// GetCIRun returns the CI run result for the given owner/module/commit triple.
	//
	// Returns NOT_FOUND if the module does not exist, the caller cannot read it,
	// or no run record exists for that commit. The last case covers commits
	// pushed while linting was disabled, and the initial commit that
	// CreateModuleByName seeds, which runs no checks.
	GetCIRun(context.Context, *connect.Request[v1.GetCIRunRequest]) (*connect.Response[v1.GetCIRunResponse], error)
}

// NewCIServiceClient constructs a client for the hades.api.registry.v1.CIService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCIServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CIServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	cIServiceMethods := v1.File_api_registry_v1_ci_proto.Services().ByName("CIService").Methods()
	return &cIServiceClient{
		getCIRun: connect.NewClient[v1.GetCIRunRequest, v1.GetCIRunResponse](
			httpClient,
			baseURL+CIServiceGetCIRunProcedure,
			connect.WithSchema(cIServiceMethods.ByName("GetCIRun")),
			connect.WithClientOptions(opts...),
		),
	}
}

// cIServiceClient implements CIServiceClient.
type cIServiceClient struct {
	getCIRun *connect.Client[v1.GetCIRunRequest, v1.GetCIRunResponse]
}

// GetCIRun calls hades.api.registry.v1.CIService.GetCIRun.
func (c *cIServiceClient) GetCIRun(ctx context.Context, req *connect.Request[v1.GetCIRunRequest]) (*connect.Response[v1.GetCIRunResponse], error) {
	return c.getCIRun.CallUnary(ctx, req)
}

// CIServiceHandler is an implementation of the hades.api.registry.v1.CIService service.
type CIServiceHandler interface {
	// GetCIRun returns the CI run result for the given owner/module/commit triple.
	//
	// Returns NOT_FOUND if the module does not exist, the caller cannot read it,
	// or no run record exists for that commit. The last case covers commits
	// pushed while linting was disabled, and the initial commit that
	// CreateModuleByName seeds, which runs no checks.
	GetCIRun(context.Context, *connect.Request[v1.GetCIRunRequest]) (*connect.Response[v1.GetCIRunResponse], error)
}

// NewCIServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCIServiceHandler(svc CIServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	cIServiceMethods := v1.File_api_registry_v1_ci_proto.Services().ByName("CIService").Methods()
	cIServiceGetCIRunHandler := connect.NewUnaryHandler(
		CIServiceGetCIRunProcedure,
		svc.GetCIRun,
		connect.WithSchema(cIServiceMethods.ByName("GetCIRun")),
		connect.WithHandlerOptions(opts...),
	)
	return "/hades.api.registry.v1.CIService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CIServiceGetCIRunProcedure:
			cIServiceGetCIRunHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCIServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCIServiceHandler struct{}

func (UnimplementedCIServiceHandler) GetCIRun(context.Context, *connect.Request[v1.GetCIRunRequest]) (*connect.Response[v1.GetCIRunResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("hades.api.registry.v1.CIService.GetCIRun is not implemented"))
}
